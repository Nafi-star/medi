import React from 'react';
import { ProfilePage } from '../pages/ProfilePage';

// Reuse existing profile/settings page for now
export const Settings: React.FC = () => {
  return <ProfilePage />;
};

